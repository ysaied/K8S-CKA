apiVersion: apps/v1
kind: ReplicaSet
metadata:
  name: rs-nginx-04
  labels:
    app: my_web_rs-04
spec:
  replicas: 3
    matchLabels:
      app: my_web_rs-04
  template:
    spec:
      containers:
      - name: nginx
        image: nginx
    metadata:
      labels:
        tier: my_web_rs-04